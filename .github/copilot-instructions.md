# Copilot Instructions

## Project Guidelines
- For major engine work, use milestone-based delivery with review gates, prioritize authoritative server/client networking first, use a hybrid plugin SDK (static core + optional dynamic plugins), target 120 FPS on high-end GPUs, and require a full automated test matrix including performance baselines before considering features complete.